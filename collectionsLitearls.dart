void main(List<String> args) {
  // List literal
  var numbers = [1, 2, 3];

// Set literal
  var uniqueNumbers = {1, 2, 3};

// Map literal
  var person = {'name': 'John', 'age': 30};

// Runes (UTF-32)
  var heart = '\u2665';
  print(heart);

  print(numbers);
  print(uniqueNumbers);
  print(person.entries);
   print(person);
}
